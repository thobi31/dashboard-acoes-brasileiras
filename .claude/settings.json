{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash -c \"cd '/c/Users/Thobias de Andrade/Desktop/projeto do claude code' && git add -A && (git diff --staged --quiet || (git commit -m 'auto: atualização via Claude Code' && git push))\" 2>/dev/null || true",
            "timeout": 30,
            "statusMessage": "Sincronizando com GitHub..."
          }
        ]
      }
    ]
  },
  "permissions": {
    "allow": [
      "Bash(git *)"
    ]
  }
}
